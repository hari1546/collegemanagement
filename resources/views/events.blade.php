<section class="section" id="menu">
    <div class="container">
        <div class="row">
            <div class="col-lg-4">
                <div class="section-heading">
                    <h6>Our Menu</h6>
                    <h2>Our selection of cakes with quality taste</h2>
                </div>
            </div>
        </div>
    </div>
</section>
{{-- @foreach ($iterm_data as $iterm )
<tr>
    <td>{{$iterm->name}}</td>
    <td>{{$iterm->department_name}}</td>
    <td>{{$iterm->year}}</td>
    <td>{{$iterm->category}}</td>
    <td>{{$iterm->date}}</td>
    <td>{{$iterm->duration}}</td>
    <td>{{$iterm->sponsor}}</td>
    <td><img height="200" width="200" src="/eventsimage/{{$iterm->image}}" alt="events images"></td>
    <td><a href="{{url('/showmodel',$iterm->id)}}">View</a></td>
    <td><a href="{{url('/deletemodel',$iterm->id)}}">Delete</a></td>
    <td><a href="{{url('/updateview',$iterm->id)}}">Update</a></td>


</tr>
@endforeach --}}
